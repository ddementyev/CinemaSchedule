@model CinemaSchedule.Models.BaseData

@using (Html.BeginForm("DeleteMovie", "Cinema", FormMethod.Post))
{

        <br />
        @Html.TextBoxFor(a=>a.Theater)
        <br />

        @Html.TextBoxFor(a=>a.MovieData.MovieTitle)
        <br />
        @Html.TextBoxFor(a=>a.Date)
        <br />

        @Html.DropDownListFor(a=>a.SelectedSession, Model.MovieData.MovieSessions.Select(a => new SelectListItem()
   {
       Text = a,
       Value = string.Format("{0:hh\\:mm}", a)

   }), new { size = 5, style = "width: 150px;" })

        <br />


    //var datawer = new Sessions()
    //{
    //    Theater = cinema.Theater,
    //    Movie = cinema.Movies.FirstOrDefault().MovieTitle,
    //    Date = Model.DateTime,
    //    Time = Model.SelectedSession
    //};

        @*@Html.ActionLink("Удалить выбранный сеанс", "DeleteMovie", "Cinema", datawer, null)*@
        @Html.TextBox("Edit", "Удалить выбранный сеанс", new { type = "submit" })
        <br />
        @Html.TextBox("Edit", "Удалить фильм и все сеансы", new { type = "button" })
        <br />
        @Html.TextBox("Edit", "Добавить сеанс", new { type = "button" })
}
